# Credentials for HTTP Basic authentication, one pair per line.
# Copy this file to config/credentials.txt and change the password.
# Blank lines and lines starting with # are ignored.

admin:change-this-password
