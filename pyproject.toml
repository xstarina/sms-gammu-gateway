# Проект разворачивается контейнером, а не устанавливается как пакет,
# поэтому здесь только настройки инструментов разработки.

[tool.pytest.ini_options]
testpaths = ["tests"]
# Корень проекта в sys.path, чтобы тесты находили пакет sms_gateway
pythonpath = ["."]
addopts = "-q"
