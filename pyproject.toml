# The project is deployed as a container rather than installed as a package,
# so this file only carries development tool settings.

[tool.pytest.ini_options]
testpaths = ["tests"]
# Project root on sys.path so tests can import the sms_gateway package
pythonpath = ["."]
addopts = "-q"
